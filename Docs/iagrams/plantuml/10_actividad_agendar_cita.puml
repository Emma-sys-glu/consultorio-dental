@startuml
title Diagrama de actividad - Agendar cita

skinparam shadowing false

start

:Usuario abre formulario de cita;
:Sistema carga pacientes y dentistas;
:Usuario captura paciente, dentista,\nfecha, horario y motivo;

if (Formulario valido?) then (si)
  :Verificar token CSRF;
  :Validar datos en Laravel;

  if (Paciente existe?) then (si)
    if (Dentista existe?) then (si)
      :Crear registro en citas;
      :Guardar en PostgreSQL;
      :Redirigir al listado de citas;
      :Mostrar mensaje de exito;
    else (no)
      :Mostrar error de dentista invalido;
    endif
  else (no)
    :Mostrar error de paciente invalido;
  endif
else (no)
  :Mostrar errores de validacion;
endif

stop

@enduml
